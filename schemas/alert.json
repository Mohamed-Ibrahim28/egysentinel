{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Alert",
  "description": "Output schema for the Alert Agent",
  "type": "object",
  "required": ["priority", "summary", "recommended_action"],
  "properties": {
    "priority": {
      "type": "string",
      "enum": ["low", "medium", "high", "critical"],
      "description": "Risk priority level of the alert"
    },
    "summary": {
      "type": "string",
      "maxLength": 200,
      "description": "One sentence summary of the suspicious activity"
    },
    "recommended_action": {
      "type": "string",
      "enum": ["investigate", "monitor", "escalate", "freeze"],
      "description": "Recommended action for the compliance officer"
    }
  },
  "additionalProperties": false
}